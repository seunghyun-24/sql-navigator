# Agent Playbook — SQL Navigator를 Claude와 만들며 배운 것

> 이 프로젝트(v0.1 → v0.9)는 코드 한 줄까지 전부 AI Agent(Claude)와의 협업으로 만들어졌다.
> 이 문서는 그 과정에서 실제로 쓰인 활용 방식, 효과가 있었던 것, 아쉬웠던 것을 정리하고,
> 이를 "Agent를 똑똑하게 쓰는 개발자의 지침"으로 일반화한 리포트다.

---

## 1. 전제: Agent는 루프다

Agent를 한 번의 질문-답변 기계로 쓰면 성능의 절반도 못 쓴다.
Agent의 본질은 **루프(loop)** 다.

```
계획 → 실행 → 관찰(테스트/실행 결과) → 수정 → 다시 실행
```

이 루프가 잘 돌려면 두 가지가 필요하다.

- **루프가 돌 수 있는 환경** — Agent가 스스로 결과를 확인할 수단 (테스트, 실행, 린트)
- **루프가 이탈하지 않게 하는 궤도** — 규칙, 계약, 문서 (= 하네스)

SQL Navigator 개발은 이 두 가지를 의도적으로 설계한 사례다.

---

## 2. 하네스 엔지니어링 — 이 프로젝트에서 실제로 한 것

하네스(harness)는 Agent를 감싸는 실행 환경과 제약의 총체다.
모델을 바꾸는 것보다 하네스를 다듬는 것이 결과를 훨씬 크게 바꾼다.

### 2.1 CLAUDE.md — 세션마다 리셋되는 기억의 보정

Agent는 세션이 끝나면 잊는다. 그래서 프로젝트 루트에 `CLAUDE.md`를 두고
**모든 세션이 같은 규칙에서 출발**하게 했다.

핵심은 규칙을 "행동 가능한 금지/허용"으로 쓴 것이다.

```
- Regex(및 문자열 검색)로 SQL을 분석하지 않는다 — 항상 AST
- Analyzer는 AST를 수정하면 안 된다 — 변환이 필요하면 새 AST를 생성한다
- SQL은 브라우저를 떠나지 않는다 — 서버 전송 금지
```

"좋은 코드를 짜라" 같은 모호한 지시는 하네스가 아니다.
**위반 여부를 기계적으로 판정할 수 있는 문장**이어야 Agent가 지킬 수 있다.

### 2.2 docs/ — 컨텍스트의 계층화

모든 문서를 한 파일에 넣지 않고 역할별로 쪼갰다.

| 문서 | 역할 | Agent에게 주는 효과 |
|---|---|---|
| vision.md | 무엇을/왜 만드는가 | 기능 제안이 방향을 벗어나지 않음 |
| principles.md | 불변 원칙 6개 | 어떤 구현이든 통과해야 할 필터 |
| architecture.md | Layer 책임 + 데이터 계약 | "이 코드가 어느 층에 속하는가" 판단 기준 |
| analyzer.md | Rule Interface 명세 | 확장 지점을 하나로 고정 |
| rules.md | Rule 카탈로그 + 추가 절차 | 새 기능 추가가 절차화됨 |
| roadmap.md | 버전별 범위 | 한 세션의 작업 단위를 제한 |

`CLAUDE.md`에는 "항상 vision/architecture/principles를 먼저 읽는다"고 명시해
**Agent가 매 세션 컨텍스트를 스스로 복원**하게 했다. 사람이 매번 설명하지 않는다.

### 2.3 데이터 계약 — 경계를 좁혀서 오류를 가두기

Pyodide(Python) ↔ JS 경계는 "직렬화 가능한 JSON만 통과"로 못 박았다.
`AnalysisResult`, `Finding`, `DiffResult`의 필드를 문서에 타입 수준으로 정의했다.

효과: Agent가 Python 쪽을 아무리 고쳐도 **경계 계약만 지키면 UI가 깨지지 않는다.**
Agent의 실수 반경(blast radius)이 Layer 하나로 제한된다.

### 2.4 확장 지점의 단일화 — Rule Interface

"새 검사 추가 = 새 Rule 파일 + Registry 등록. Engine은 건드리지 않는다."

이 한 줄이 프로젝트 후반(v0.6에서 Rule 8종 일괄 추가)의 품질을 결정했다.
Agent에게 "기능을 추가해"라고 하면 어디든 손을 대지만,
**확장 지점이 하나로 고정되어 있으면 손댈 곳이 하나뿐**이다.
실제로 v0.6은 Engine/Parser 수정 0줄로 Rule 커버리지를 2배로 늘렸다.

### 2.5 테스트 요건의 정형화 — 루프의 종료 조건

Rule마다 테스트 3종을 강제했다: 탐지 케이스, 비탐지(false positive 방지) 케이스,
Explainability(이유/예시/수정 존재) 검증.

이것이 Agent 루프의 **종료 조건(exit condition)** 역할을 했다.
"다 됐어요"라는 Agent의 자기 보고 대신, `pytest` 통과가 완료의 정의가 된다.
Fix Apply(v0.9)에서는 한 단계 더 나아가 "fixSql을 parse했을 때 에러가 없고,
같은 Rule을 다시 적용했을 때 Finding이 없어야 한다"는 **자기 검증 루프**를 요건화했다.

---

## 3. 루프 엔지니어링 — 작업 단위 설계

### 3.1 버전 = 루프 하나

roadmap.md의 v0.1~v0.9는 단순한 계획표가 아니라 **Agent 작업 단위의 크기 조절 장치**였다.

- v0.1: 파이프라인만 (SQL 입력 → 포맷 + 트리)
- v0.2: Rule Engine + 초기 Rule 7종
- v0.3~0.4: 시각화 (Join Graph, Query Flow)
- v0.5: Schema Context
- v0.6: Rule 확장 (Engine 무수정)
- v0.7~0.9: Position Mapper, Diff, Fix Apply

각 버전에는 검증 가능한 "목표" 문장이 붙어 있다
(예: v0.4 — "Explain Plan 없이 쿼리 동작을 설명할 수 있다").
Agent에게 "전부 만들어줘"가 아니라 **한 번에 한 루프**를 돌린 것이 핵심이다.

### 3.2 임시방편의 졸업 계획

v0.2에서 위치 정보(`location`)를 정확히 만들 수 없어 `null + snippet`으로 타협했다.
중요한 건 그 타협을 **문서에 명시하고 졸업 시점(v0.7 Position Mapper)을 예약**한 것이다.
Agent와의 협업에서 임시방편은 사라지지 않는다 — 문서에 적어두지 않으면 영구화된다.

---

## 4. 무엇이 좋았나

**아키텍처 일관성이 유지됐다.** 9개 버전, 수십 세션에 걸쳐 Layer 침범이 발생하지
않았다. Component에 분석 로직이 들어가거나 Analyzer가 문자열을 만지는 일이
하네스 규칙 덕에 리뷰 이전에 차단됐다.

**확장 비용이 상수로 유지됐다.** Rule 1개 추가 비용이 프로젝트 초기와 후기에
동일했다(파일 1개 + 테스트 1개 + 등록 1줄). 보통 프로젝트는 커질수록 추가 비용이
증가하는데, 확장 지점 고정이 이를 막았다.

**Agent가 스스로 검증했다.** 테스트 요건이 정형화되어 있어 "구현 → pytest →
실패 → 수정" 루프를 사람 개입 없이 돌 수 있었다. 사람의 역할은 루프의 결과 검토로
축소됐다.

**설계 논의의 질이 높았다.** 문서가 판단 기준을 제공하니 "이건 Analyzer의 일인가
Renderer의 일인가" 같은 논의가 취향 싸움이 아니라 architecture.md 조회로 끝났다.

## 5. 무엇을 개선하면 좋았나

**프론트엔드 검증 루프가 약했다.** Python 쪽은 pytest로 루프가 닫혔지만, React/UI는
자동 검증 수단이 없어 사람 눈으로 확인해야 했다. 개선: Vitest + Testing Library를
초기에 넣거나, Agent에게 스크린샷 검증 루프를 제공했어야 한다.
**교훈: 루프가 닫히지 않는 영역이 품질의 병목이 된다.**

**데이터 계약의 기계적 검증이 없었다.** `AnalysisResult` 계약은 문서로만 존재했고
JSON Schema나 타입 생성으로 강제되지 않았다. Python이 내보내는 JSON과 `types.ts`가
어긋나면 런타임에야 발견된다. 개선: 계약을 스키마 파일로 두고 양쪽에서 검증.

**세션 간 결정 기록(ADR)이 없었다.** "왜 이렇게 했는가"는 문서에 남았지만
"무엇을 고려하고 버렸는가"는 대화 속에 증발했다. 다음 세션의 Agent가 이미 기각된
설계를 다시 제안하는 일이 생긴다. 개선: 가벼운 ADR 파일 한 장이면 충분하다.

**CI가 없었다.** 하네스의 최종 형태는 CI다. 사람이 "테스트 돌렸어?"라고 묻는 대신
푸시 시점에 기계가 묻게 해야 한다. Analyzer가 순수 Python이라 CI 실행이 쉬운
구조인데도 활용하지 않았다.

---

## 6. 지침: Agent를 똑똑하게 쓰는 개발자

이 프로젝트의 경험을 일반화하면 다음과 같다.

**1. 규칙은 판정 가능하게 써라.**
"깨끗하게"가 아니라 "Regex로 SQL을 분석하지 않는다". Agent에게 규칙은
가치관이 아니라 컴파일 타임 체크에 가까워야 한다.

**2. 기억을 파일로 외부화하라.**
Agent의 컨텍스트는 세션마다 리셋된다. CLAUDE.md + 계층화된 docs가
Agent의 장기 기억이다. "매번 설명하는 내용"이 생기면 즉시 문서로 옮겨라.

**3. 확장 지점을 하나로 고정하라.**
Plugin/Rule/Handler 패턴은 사람에게도 좋지만 Agent에게는 필수에 가깝다.
"어디를 고쳐야 하는가"의 답이 하나면 Agent는 거의 틀리지 않는다.

**4. 루프의 종료 조건을 기계에게 맡겨라.**
완료의 정의는 Agent의 자기 보고가 아니라 테스트 통과다.
테스트가 없는 영역은 Agent에게 "감으로 됐다고 말해도 되는 영역"이 된다.

**5. 작업 단위를 버전으로 썰어라.**
한 루프에 하나의 검증 가능한 목표. "전부 다"는 루프가 아니라 도박이다.

**6. 경계에 계약을 세워라.**
Layer 간 데이터 계약을 타입/스키마 수준으로 정의하면 Agent의 실수가
경계 안에 갇힌다. 계약은 문서보다 기계 검증이 낫다.

**7. 임시방편에 졸업 날짜를 붙여라.**
타협은 하되, 문서에 "v0.X에서 해소"라고 적어라. 적지 않은 부채는
Agent가 정상 상태로 학습한다.

**8. Agent가 못 보는 것을 파악하라.**
이 프로젝트에서 Agent는 pytest 결과는 봤지만 렌더링된 화면은 못 봤다.
Agent의 관찰 범위 밖 영역이 어디인지 알고, 그곳에 사람의 리뷰를 집중하라.

---

## 7. 사용 후기

솔직한 후기 한 줄: **Agent는 부하 직원이 아니라 대단히 성실한데 기억상실증이 있는
시니어 동료**다.

- 아키텍처 문서를 던져주면 나보다 잘 지킨다. 안 던져주면 매 세션 새로운 유파를 창시한다.
- "테스트 통과할 때까지 고쳐줘"는 마법의 주문이다. 진짜로 통과할 때까지 고친다.
- 가장 시간을 아낀 순간은 코드를 짜준 순간이 아니라, v0.6에서 Rule 8종을
  추가하는데 내가 리뷰할 게 Rule 파일과 테스트뿐이었던 순간이다. 하네스를
  잘 짜면 리뷰가 얇아진다. 이게 진짜 생산성이다.
- 반대로 가장 답답했던 순간은 UI 미세 조정. "버튼이 좀 이상해"를 말로 설명하는
  건 사람에게도 어렵다. 보는 눈(스크린샷 루프)을 주지 않은 내 잘못이 크다.
- 결론: 모델이 얼마나 똑똑한가보다 **내가 궤도를 얼마나 잘 깔았는가**가
  결과물을 결정했다. Agent 활용 능력이란 결국 하네스 설계 능력이다.

별점: ★★★★☆ — 별 하나는 CI 안 붙인 나 자신에게서 차감.
